import React, { Component } from 'react'

export default class HamburberButton extends Component {
    render() {
        return (
            <button type="button" id="mobile-nav-toggle"><i className="fa fa-bars" /></button>
        )
    }
}
